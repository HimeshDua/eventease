// Placeholder — real feature tests are added per screen as they are built.
// The default counter test was removed because the app now requires Firebase.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
